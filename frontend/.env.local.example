# API Configuration
# Backend API URL (exposed to client via NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket Configuration
# WebSocket URL for real-time communication (exposed to client via NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_WS_URL=ws://localhost:8000
